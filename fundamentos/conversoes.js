// topo de dado
// booleanos

// conversão implicíta
const numero = 456;
const numeroString = "456";

console.log(numero + Number(numeroString))

// = atribuir valor a uma variável
// == comparação pelo valor (somente o que está dentro da variável)
// === compara não só o valor mas tbm o tipo ( se é string ou number), só atrinuira true se os dois
// forem iguais, 2 tipo numeber, ou os dois tipo string

// conversão explicita

// number () = transforma string em numero (somente strings que contenha somente números)
// string () = tansforma numero em string
